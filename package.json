{
  "name": "chaossquadbot",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "bot": "npm run dev --workspace=apps/bot",
    "dashboard": "npm run dev --workspace=apps/dashboard"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
